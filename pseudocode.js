// First event rolls the dice to determine which player goes first.
/*  if user1>user2 {
    user1's Turn function initiates
    else user2  Turn function initiates}

    user1 or user2 Turn function call the roll dice function
    and assings value to users position.
    
    The user position current number/value makes the users game piece display on that number on the board

) */